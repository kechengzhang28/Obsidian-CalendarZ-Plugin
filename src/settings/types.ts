/**
 * Settings type definitions
 * Re-export from core types for backward compatibility
 */

export type {
	Language,
	MonthFormat,
	TitleFormat,
	WeekStart,
	DateSource,
	DisplayMode,
	StatisticsType,
	CalendarZSettings,
} from "../core/types";
